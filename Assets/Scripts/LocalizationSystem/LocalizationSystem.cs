using Common.Logger;
using UnityEngine;

namespace LocalizationSystem
{
    public static class LocalizationSystem
    {
        public static string Get(string lid, string defaultValue)
        {
            Log.Todo("сделать выборку из таблицы локализации");
            return defaultValue;
        } 
    }
}